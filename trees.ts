import { Vector, CanvasApp } from './main.js';

class TreeCanvasApp extends CanvasApp {
  private

  constructor() {
    super();
  }

  protected addListeners(): void {
    super.addListeners();
    console.log('hi');
  }
}

new TreeCanvasApp();



class Seed {
  constructor(x:number, y:number, ) {
    
  }
}

class Tree {

}
